---
// src/components/sections/BirthdaySection.astro
const birthdayPhotos = [
  "/cumpleaños/niños.webp",
  "/cumpleaños/image-Photoroom.webp",
  "/cumpleaños/image-Photoroom (2).webp",
  "/cumpleaños/9b5feaa6-6e88-4e06-b8fa-caa21cb05c9b.webp"
];
---

<section class="birthday-section">
  <div class="container split-layout align-center">
    <div class="text-content">
      <h3 class="section-subtitle">20 AÑOS CELEBRANDO</h3>
      <h2 class="section-title">EL REY DE LOS<br><span class="text-outline">CUMPLEAÑOS</span></h2>
      <p>Ven a celebrar con nosotros. Tenemos todo listo para que sea un día inolvidable tanto para los peques como para los padres.</p>
      
      <ul class="features-list">
        <li>
          <div class="feature-icon">
            <svg xmlns="http://www.w3.org/2000/svg" width="28" height="28" viewBox="0 0 24 24" fill="currentColor"><path d="M21 11c0-.55-.45-1-1-1h-1V5c0-1.1-.9-2-2-2H7c-1.1 0-2 .9-2 2v5H4c-.55 0-1 .45-1 1v9c0 1.1.9 2 2 2h14c1.1 0 2-.9 2-2v-9zm-8-6h2v3h-2V5zm-4 0h2v3H9V5zM7 5h2v3H7V5zm12 15H5v-7h14v7z"/><path d="M9 13h2v4H9zm4 0h2v4h-2z"/></svg>
          </div>
          <div>
            <strong>Castillo Hinchable</strong><br>
            <small>Diversión asegurada</small>
          </div>
        </li>
        
        <li>
          <div class="feature-icon">
            <svg xmlns="http://www.w3.org/2000/svg" width="28" height="28" viewBox="0 0 24 24" fill="currentColor"><path d="M20.2 17.6l-2.8-7.7c-.5-1.4-2.1-2.1-3.5-1.6l-1.6.6c.4.8.6 1.7.6 2.7 0 2.5-1.5 4.6-3.7 5.6l1.3 3.6c.1.3.4.5.8.5h.3l7.4-2.7c.4-.2.5-.7.4-1z"/><path d="M10.7 13.9c-1.4.6-2.9.6-4.3 0C5.3 13.4 4.5 12.5 4.1 11.4L2.8 7.7C2.3 6.3 3 4.7 4.4 4.2L6 3.6c.8-.3 1.7-.1 2.3.4l.1.1c.7.5 1 1.3 1.1 2.1l2.9 8c-.5-.1-1.1-.2-1.7-.3zM4 20h6c.6 0 1 .4 1 1s-.4 1-1 1H4c-.6 0-1-.4-1-1s.4-1 1-1z"/></svg>
          </div>
          <div>
            <strong>Zona de Padres</strong><br>
            <small>Relax y buenas tapas</small>
          </div>
        </li>

        <li>
          <div class="feature-icon">
            <svg xmlns="http://www.w3.org/2000/svg" width="28" height="28" viewBox="0 0 24 24" fill="currentColor"><path d="M12 2C8.43 2 5.23 3.54 3.01 6L12 22l8.99-16C18.78 3.55 15.57 2 12 2zM7 7c0-1.1.9-2 2-2s2 .9 2 2-.9 2-2 2-2-.9-2-2zm5 8c-1.1 0-2-.9-2-2s.9-2 2-2 2 .9 2 2-.9 2-2 2z"/></svg>
          </div>
          <div>
            <strong>Menús Especiales</strong><br>
            <small>Meriendas y cenas</small>
          </div>
        </li>
      </ul>

      <div class="stats-row">
        <div class="stat-item">
          <span class="stat-number">+20</span>
          <span class="stat-label">Años de Experiencia</span>
        </div>
      </div>
    </div>
    
    <div class="birthday-slider-container">
        <div class="birthday-slider-wrapper">
            <div class="slider-track">
              {[...birthdayPhotos, ...birthdayPhotos, ...birthdayPhotos].map((photo) => (
                <div class="slide-item">
                  <img src={photo} alt="Celebración Cumpleaños" loading="lazy" />
                </div>
              ))}
            </div>
        </div>
    </div>
  </div>
</section>

<style>
  section { position: relative; width: 100%; }
  
  .section-subtitle { 
    color: var(--color-accent); 
    font-family: var(--font-body); 
    font-weight: 700; 
    letter-spacing: 2px; 
    margin-bottom: 10px; 
    text-transform: uppercase; 
    font-size: 0.9rem; 
  }
  
  .section-title { 
    font-size: clamp(2.5rem, 5vw, 4rem); 
    line-height: 1; 
    margin-bottom: 20px; 
  }
  
  .text-outline { 
    -webkit-text-stroke: 1px var(--color-text-main); 
    color: transparent; 
  }
  
  .birthday-section { 
    padding: 100px 0; 
    background-color: #0f0f0f; 
    overflow: hidden; 
  }
  
  .split-layout { 
    display: grid; 
    grid-template-columns: 1fr 1fr; 
    gap: 60px; 
  }
  
  .align-center { 
    align-items: center; 
  }

  /* LISTA DE CARACTERÍSTICAS */
  .features-list { 
    list-style: none; 
    margin-top: 30px; 
    display: flex; 
    flex-direction: column; 
    gap: 25px; 
  }
  
  .features-list li { 
    display: flex; 
    align-items: center; 
    gap: 20px; 
  }
  
  /* CÍRCULO DEL ICONO - Mejorado */
  .feature-icon { 
    width: 64px;
    height: 64px;
    background: rgba(255, 193, 7, 0.1); /* Fondo amarillo muy sutil */
    border: 1px solid rgba(255, 193, 7, 0.3); /* Borde amarillo sutil */
    border-radius: 50%; 
    display: flex;
    align-items: center;
    justify-content: center;
    color: var(--color-accent); /* Icono en amarillo fuerte */
    flex-shrink: 0;
    transition: all 0.3s ease;
    box-shadow: 0 4px 20px rgba(0,0,0,0.3); /* Sombra para dar profundidad */
  }

  /* Hover más potente */
  .features-list li:hover .feature-icon {
    background: var(--color-accent);
    color: #000; /* Icono pasa a negro */
    border-color: var(--color-accent);
    transform: translateY(-3px) scale(1.05);
    box-shadow: 0 10px 25px rgba(255, 193, 7, 0.4); /* Resplandor amarillo */
  }
  
  .features-list strong { 
    font-family: var(--font-heading); 
    font-size: 1.2rem; /* Un poco más grande */
    color: #fff; 
    text-transform: uppercase; 
    letter-spacing: 0.5px;
  }
  
  .features-list small { 
    color: var(--color-stone-grey); 
    font-size: 0.95rem;
  }

  /* ESTADÍSTICAS */
  .stats-row { 
    margin-top: 40px; 
    border-top: 1px solid rgba(255,255,255,0.1); 
    padding-top: 20px; 
  }
  
  .stat-number { 
    font-size: 3rem; 
    font-family: var(--font-heading); 
    color: var(--color-accent); 
    font-weight: 700; 
    line-height: 1; 
    display: block;
  }
  
  .stat-label { 
    font-family: var(--font-heading); 
    text-transform: uppercase; 
    color: var(--color-stone-grey); 
    letter-spacing: 1px; 
  }

  /* SLIDER */
  .birthday-slider-container { width: 100%; padding: 80px 0; overflow: hidden; mask-image: linear-gradient(to right, transparent, black 10%, black 90%, transparent); }
  .birthday-slider-wrapper { position: relative; width: 100%; height: 400px; }
  .slider-track { display: flex; gap: 40px; position: absolute; left: 0; top: 50%; transform: translateY(-50%); animation: scroll-horizontal-centered 30s linear infinite; }
  @keyframes scroll-horizontal-centered { 0% { transform: translate(0, -50%); } 100% { transform: translate(-50%, -50%); } }
  
  .birthday-slider-container:hover .slider-track { animation-play-state: paused; }
  .slide-item { width: 260px; height: 400px; flex-shrink: 0; border-radius: 15px; overflow: hidden; border: 4px solid #fff; box-shadow: 0 20px 40px rgba(0,0,0,0.6); transform: rotate(-3deg); transition: transform 0.3s ease, border-color 0.3s ease; background: #222; margin: 20px 0; }
  .slide-item:nth-child(even) { transform: rotate(3deg); }
  .slide-item:hover { transform: scale(1.08) rotate(0deg) !important; z-index: 10; border-color: var(--color-accent); box-shadow: 0 30px 60px rgba(0,0,0,0.8); }
  .slide-item img { width: 100%; height: 100%; object-fit: cover; }

  @media (max-width: 968px) {
    .split-layout { grid-template-columns: 1fr; text-align: center; }
    .features-list { align-items: center; text-align: left; }
    .features-list li { width: fit-content; }
    .birthday-slider-container { padding: 60px 0; }
    .birthday-slider-wrapper { height: 320px; }
    .slide-item { width: 210px; height: 320px; margin: 10px 0; }
    .slider-track { gap: 30px; }
  }
</style>